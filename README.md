 Callibri EMG Tracker (Flutter)

- Real-time EMG with Callibri (neurosdk2)
- Exercise presets, thresholds (Hi/Lo), MVC calibration


Quick start
```bash
flutter pub get
cd ios && pod install && cd ..
flutter run -d ios

